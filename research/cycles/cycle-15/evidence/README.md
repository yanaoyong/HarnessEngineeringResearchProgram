# Cycle 15 证据准备（Evidence Preparation）

> 状态：PREPARED · NO EVIDENCE CLAIMS

本目录当前没有 `SRC-*`、Project Artifact ID 或 `EVD-*`。Cycle 15 复用 Cycle 4、6、9–14 已登记的来源作为问题形成材料，不复制 Source ID，也不将 Source Registry entry 升级为 Evidence Claim。

真实执行时才可登记：固定 myharness commit、Coverage Matrix、8 个 dimension packets、deterministic checks、Agent Run 与 evaluator artifact，并分别分配 Project Artifact / Run ID。任何进入 Cycle 16 的复用 claim 必须派生 scoped `EVD-*`，引用这些底层 artifacts。路径、目录或 Reference Pattern 存在不能单独证明架构问题；路径不存在也不能单独证明 capability 不存在。
