# Research Cycles

进入一个研究循环时才创建对应目录。

推荐结构：

```text
week-01/
├── research-note.md
├── experiments/
│   └── EXP-W01-01.md
└── evidence/
```

不要预先创建 `week-01` 到 `week-16` 的全部目录。

如果路线发生变化，例如 Week 7 提前借用 Week 11 的方法，仍以当前核心 Research Question 所属循环记录，并在 Research Note 与 Route Review 里说明路线变化。
