# Cycle 03 实验工作区（Experiment Workspace）

> 状态（Status）：PREPARED · NOT EXECUTED

本目录为后续实现预先准备。计划实验：

- `EXP-C03-01` · Claude Code Context Lifecycle Trace
- `EXP-C03-02` · Controlled Compaction Recovery Comparison

真实执行开始后，使用 [实验记录模板](../../../templates/experiment-record.template.md) 创建独立 Experiment Record，并为每次执行保存独立 Run Metadata。`EXP-C03-01` 使用 `OBSERVATION_ONLY`，只定位 transition 与 confounder，Experiment Result 为 `NOT APPLICABLE · OBSERVATION ONLY`；`EXP-C03-02` 使用 `HYPOTHESIS_RESULT`，其 conversation-only / Artifact 配对 Run 是 Cycle 3 Exit Criteria 的必做项。在此之前不得填写结果或派生 `EVD-*`。
